#include<stdio.h>
#include<conio.h>

main ()
{
	int n;
	clrscr();

	printf("Enter any namber:");
	scanf("%d",&n);

	if (n<0)
	{
		printf("%d is negative !!",n);
	}

	else if (n>0)
	{
		printf("%d is positiv !!",n);
	}

	else
	{
		printf("%d is neutral !!",n);
	}

	getch();








}